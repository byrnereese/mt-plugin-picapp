id: PicApp
name: 'PicApp'
description: 'A plugin that provides integration with the PicApp image distribution platform.'
version: 0.1

object_types:
  asset.picapp: MT::Asset::PicApp

config_settings:
  PicAppServerURL: 
    default: 'http://api.picapp.com/Api/ws.asmx'
  PicAppCachePath: 
    default: ''

options:
  picapp_api_key:
    label: 'API Key'
    hint: 'Enter the API Key you received from PicApp, or <a href="http://www.picapp.com/ApiReg.aspx">apply for one now</a>.'
    type: text
    scope: blog

applications:
  cms:
    methods:
      picapp_find: $PicApp::PicApp::Plugin::find
      picapp_find_results: $PicApp::PicApp::Plugin::find_results
      picapp_asset_options: $PicApp::PicApp::Plugin::asset_options
    menus:
      create:picapp:
        label: 'PicApp Asset'
        order: 303
        dialog: 'picapp_find'
        view: 'blog'



